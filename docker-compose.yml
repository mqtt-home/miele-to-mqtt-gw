version: '2'

services:
  mielemqtt:
    hostname: mielemqtt
    image: pharndt/mielemqtt:${VERSION}
    build: ./app
    volumes:
      - ./production/config:/var/lib/miele-to-mqtt-gw:ro
    restart: "no"
